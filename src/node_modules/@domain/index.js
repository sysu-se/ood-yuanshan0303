// Barrel re-export so that `@domain/index` resolves correctly
// via rollup's node-resolve plugin searching src/node_modules/
export { Sudoku, Game, createSudoku, createSudokuFromJSON, createGame, createGameFromJSON } from '../../domain/index.js';
